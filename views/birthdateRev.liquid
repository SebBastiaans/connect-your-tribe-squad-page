{% render 'partials/head.liquid', title: 'Squad page' %}

<ul class="filter">
  <li class="name">
    <a href="/">Naam</a>
  </li>
  <li class="birthdate">
    <div>
    <a class="bold" href="/birthdate">Geboorte datum</a>
    <a href="/birthdate">O - J</a>
    <a class="bold" href="/birthdate-jong-oud">J - O</a>
    </div>
  </li>
  <li class="border">
    <a href="/border-radius">Ronde hoek</a>
  </li>
  <li class="search">
    <form action="">
      <input type="search" placeholder="Search...">
    </form>
  </li>
</ul>

<ul class="students">
  {% for person in persons %}
    <li class="student-card" style="box-shadow:0 0 20px 5px {{ person.fav_color }}; border-radius: {{ person.fav_border_radius }}px"> 
      <a href="/student/{{ person.id }}">{{ person.name }}</a>
      <a href="/student/{{ person.id }}">{{ person.birthdate | date: '%d-%m-%Y'}}</a>

      <img
        class="person-img"
        loading="lazy"
        width="100"
        {% if person.mugshot %}
          src="{{'https://fdnd.directus.app/assets/' | append:  person.mugshot | append:'?width=300' }}"
        {% else %}
          src="{{ person.avatar }}"
        {% endif %}
        alt="{{ person.name }}"
      >
    
    </li>
  {% endfor %}
</ul>


{% render 'partials/foot.liquid', squads: squads %}